Build using Qt Creator either in Debug or Release mode
Operating system - Windows or Linux.
If gcc is used to compile for Windows - it should include std::thread and std::mutex implementation (for example, TDM-GCC)
Qt 4.8 or higher should be installed